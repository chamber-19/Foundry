name: foundry-scoring
channels:
  - defaults
dependencies:
  - python=3.13
  - pip
  - pip:
    - scikit-learn>=1.8
    - numpy>=2.4
    - joblib
    - gitpython
    - chromadb
    - scipy>=1.15
