# ML pipeline patterns

> **Historical archive:** this document describes the three-engine ML scoring pipeline (scikit-learn → PyTorch → TensorFlow) and the `EngineHandoff` contract that were removed in cleanup pass 1. It no longer reflects anything in the codebase. Use [README.md](../../README.md) for current architecture.
